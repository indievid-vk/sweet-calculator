# 🍰 Как опубликовать "Сладкий расчёт" в интернете

## 📌 Для кого эта инструкция

Эта инструкция написана простым языком для тех, кто:
- Никогда не работал с GitHub
- Не знает, что такое терминал
- Впервые публикует сайт в интернете

**Время выполнения**: 30-40 минут  
**Сложность**: ⭐⭐ (требуется внимательность)

---

## 🎯 Что вы получите в итоге

После выполнения всех шагов у вас будет:
- ✅ Работающее приложение в интернете
- ✅ Красивая ссылка типа: `https://ваш-никнейм.github.io/sweet-calculator/`
- ✅ Приложение можно установить на телефон как обычное приложение
- ✅ Работает без интернета

---

# ЧАСТЬ 1️⃣: Установка программ (только один раз)

## Шаг 1: Установите Node.js

### Зачем это нужно?
Node.js - это программа, которая нужна для "сборки" приложения перед публикацией.

### Как установить:

1. **Откройте браузер** (Chrome, Firefox, Safari - любой)

2. **Перейдите на сайт**: https://nodejs.org/

3. **Увидите две зелёные кнопки**. Нажмите на **левую кнопку** с надписью **"LTS"**
   - Файл начнёт скачиваться

4. **Найдите скачанный файл** (обычно в папке "Загрузки")
   - Для Windows: файл называется примерно `node-v20.xx.x-x64.msi`
   - Для Mac: файл называется примерно `node-v20.xx.x.pkg`

5. **Двойной клик** по скачанному файлу

6. **Установка**:
   - Нажимайте кнопку **"Далее"** (Next) на каждом экране
   - НЕ меняйте никакие настройки
   - В конце нажмите **"Установить"** (Install)
   - Подождите 1-2 минуты
   - Нажмите **"Готово"** (Finish)

7. **ОБЯЗАТЕЛЬНО перезагрузите компьютер!**

### Проверка (после перезагрузки):

**Для Windows:**
1. Нажмите клавиши `Win + R` одновременно
2. Введите: `cmd`
3. Нажмите Enter
4. Откроется чёрное окно (это терминал)
5. Введите: `node --version`
6. Нажмите Enter
7. Должно появиться: `v20.xx.x`

**Для Mac:**
1. Нажмите `Cmd + Пробел`
2. Введите: `Terminal`
3. Нажмите Enter
4. Откроется окно терминала
5. Введите: `node --version`
6. Нажмите Enter
7. Должно появиться: `v20.xx.x`

✅ **Если видите версию** - отлично, Node.js установлен!  
❌ **Если пишет "команда не найдена"** - переустановите Node.js и перезагрузите компьютер

---

## Шаг 2: Установите Git

### Зачем это нужно?
Git - программа для загрузки кода на GitHub.

### Для Windows:

1. **Откройте браузер**

2. **Перейдите на**: https://git-scm.com/download/win

3. **Скачивание начнётся автоматически**
   - Подождите, пока скачается файл

4. **Откройте скачанный файл** (двойной клик)

5. **Установка**:
   - Нажимайте **"Next"** на всех экранах
   - НЕ меняйте настройки (всё оставляйте как есть)
   - В конце нажмите **"Install"**
   - Подождите 1-2 минуты
   - Нажмите **"Finish"**

6. **Перезагрузите компьютер**

### Для Mac:

1. **Откройте Terminal** (Cmd + Пробел → введите "Terminal")

2. **Введите команду**:
   ```
   git --version
   ```

3. **Если Git не установлен**, macOS сама предложит установить инструменты разработчика
   - Нажмите **"Установить"**
   - Подождите 5-10 минут
   - Готово!

### Проверка (после перезагрузки):

**Откройте терминал** (см. Шаг 1) и введите:
```
git --version
```

Должно появиться: `git version 2.xx.x`

✅ **Если видите версию** - Git установлен!

---

## Шаг 3: Настройте Git (только первый раз)

**Откройте терминал** и введите **две команды**:

### Команда 1 - Ваше имя:
```
git config --global user.name "Ваше Имя"
```

Например:
```
git config --global user.name "Maria Ivanova"
```

**Нажмите Enter**

### Команда 2 - Ваш email:
```
git config --global user.email "ваш-email@example.com"
```

**⚠️ ВАЖНО**: Используйте тот же email, который будете использвать на GitHub!

Например:
```
git config --global user.email "maria@gmail.com"
```

**Нажмите Enter**

✅ Готово! Терминал можно закрыть.

---

# ЧАСТЬ 2️⃣: Скачивание кода

## Шаг 4: Скачайте код из Figma Make

1. **В Figma Make** найдите кнопку **"Download"** или **"Скачать"**

2. **Нажмите на неё**

3. **Выберите** "Download as ZIP" или "Скачать как ZIP"

4. **Подождите**, пока файл скачается

5. **Найдите файл** в папке "Загрузки"
   - Обычно называется примерно `sweet-calculator.zip`

---

## Шаг 5: Распакуйте и переместите проект

### Распаковка:

**Для Windows:**
1. Найдите файл `sweet-calculator.zip` в папке "Загрузки"
2. **Правый клик** на файл
3. Выберите **"Извлечь всё..."**
4. Нажмите **"Извлечь"**

**Для Mac:**
1. Найдите файл `sweet-calculator.zip` в папке "Загрузки"
2. **Двойной клик** - папка распакуется автоматически

### Перемещение в удобное место:

1. **Создайте папку** для вших проектов:
   - Например, на Рабочем столе создайте папку **"Мои проекты"**

2. **Переместите** распакованную папку `sweet-calculator` в **"Мои проекты"**

3. **Откройте папку** `sweet-calculator`

4. **Вы должны видеть**:
   - Папку `src` с файлами App.tsx, main.tsx, index.css
   - Внутри `src` папки: `components`, `styles`, `assets`, `guidelines`
   - Файлы `package.json`, `index.html`, `vite.config.ts` в корне

✅ Если видите эти файлы - отлично!

---

# ЧАСТЬ 3️⃣: Подготовка проекта

## Шаг 6: Откройте терминал В ПАПКЕ проекта

**Это важный шаг! Терминал должен быть открыт именно в папке проекта!**

### Для Windows:

1. **Откройте папку** `sweet-calculator` в Проводнике

2. **Кликните в адресную строку** (где написан путь к папке)

3. **Сотрите весь текст** и введите: `cmd`

4. **Нажмите Enter**

5. Откроется терминал **прямо в этой папке**

### Для Mac:

1. **Откройте Finder**

2. **Найдите папку** `sweet-calculator`

3. **Правый клик** на папку

4. Выберите **"Services"** → **"New Terminal at Folder"**
   - Если не видите такой опции: просто откройте Terminal и введите `cd ` (с пробелом), затем **перетащите папку** в окно терминала, нажмите Enter

### Проверка:

В терминале введите:
- **Windows**: `dir`
- **Mac**: `ls`

Вы должны увидеть список файлов проекта: `package.json`, `src`, `public` и т.д.

✅ Если видите - вы в правильной папке!

---

## Шаг 7: Установите зависимости

**В терминале** (который открыт в папке проекта) введите:

```
npm install
```

**Нажмите Enter**

**ЧТО ПРОИСХОДИТ:**
- npm скачивает все нужные библиотеки
- Это занимает 2-5 минут
- Вы увидите много текста - **это нормально!**
- НЕ закрывайте терминал!

**Когда закончится**, вы увидите что-то вроде:
```
added 423 packages in 2m
```

Затем установите ещё одну библиотеку:

```
npm install --save-dev gh-pages
```

**Нажмите Enter**

Подождите 10-20 секунд.

✅ Готово! Все нужные библиотеки установлены.

---

## Шаг 8: Добавьте изображения (важно!)

Приложение использует изображения, которые нужно правильно разместить.

1. **Создайте папки** в проекте:
   - Откройте папку проекта `sweet-calculator`
   - Создайте папку `public` (если её нет)
   - Внутри `public` создайте папку `images`

2. **Скачайте или добавьте изображения**:
   - Главное изображение: сохраните как `hero.png`
   - Переместите в `/public/images/hero.png`

3. **Обновите код** (только если используете Figma Make):
   - Откройте файл `/components/HomePage.tsx`
   - Найдите строку: `import heroImage from 'figma:asset/...'`
   - Замените на: `const heroImage = '/images/hero.png';`

📝 **Подробная инструкция**: см. файл `IMAGES_GUIDE.md`

---

## Шаг 9: Настройте конфигурацию

Нужно отредактировать 2 файла. **Можно использовать обычный Блокнот!**

### Файл 1: `vite.config.ts`

1. **Откройте файл** `vite.config.ts` в Блокноте (или любом текстовом редакторе)
   - Правый клик → Открыть с помощью → Блокнот

2. **Найдите строку** (примерно строка 6-7):
   ```typescript
   export default defineConfig({
     plugins: [react()],
   ```

3. **ДОБАВЬТЕ** после строки `plugins: [react()],` новую строку:
   ```typescript
   export default defineConfig({
     plugins: [react()],
     base: process.env.NODE_ENV === 'production' ? '/sweet-calculator/' : '/',
   ```

4. **Замените** `/sweet-calculator/` на название вашего репозитория
   - Если хотите назвать `my-sweet-app`, измените на:
   ```typescript
   base: process.env.NODE_ENV === 'production' ? '/my-sweet-app/' : '/',
   ```

5. **⚠️ ВАЖНО**: Слэши `/` в начале и конце **обязательны**!

6. **Найдите строку** (примерно строка 45):
   ```typescript
   outDir: 'build',
   ```

7. **Замените** на:
   ```typescript
   outDir: 'dist',
   ```

8. **Сохраните файл**: `Ctrl+S` (Windows) или `Cmd+S` (Mac)

### Файл 2: `package.json`

1. **Откройте файл** `package.json` в Блокноте

2. **Найдите строку** (примерно строка 3):
   ```json
   "private": true,
   ```

3. **ДОБАВЬТЕ** после неё новые строки:
   ```json
   "private": true,
   "homepage": "https://YOUR-USERNAME.github.io/sweet-calculator/",
   "type": "module",
   ```

4. **Замените** `YOUR-USERNAME` на ваш логин GitHub

5. **Найдите раздел** `"scripts"` (примерно строка 51):
   ```json
   "scripts": {
       "dev": "vite",
       "build": "vite build"
   }
   ```

6. **Замените** его на:
   ```json
   "scripts": {
       "dev": "vite",
       "build": "vite build",
       "preview": "vite preview",
       "predeploy": "npm run build",
       "deploy": "gh-pages -d dist"
   }
   ```

7. **Добавьте** после раздела `"scripts"` (перед последней `}`):
   ```json
   "scripts": {
       ...
   },
   "repository": {
       "type": "git",
       "url": "https://github.com/YOUR-USERNAME/sweet-calculator.git"
   }
   ```

8. **Замените** `YOUR-USERNAME` на ваш логин GitHub

9. **⚠️ ВАЖНО**: Следите за запятыми! JSON очень строгий к синтаксису.

10. **Сохраните файл**: `Ctrl+S` (Windows) или `Cmd+S` (Mac)

---

## Шаг 10: Проверьте, что всё работает

**В терминале** введите:

```
npm run build
```

**Нажмите Enter**

**Подождите 10-30 секунд**

**Если всё хорошо**, вы увидите:
```
vite v5.x.x building for production...
✓ 1234 modules transformed.
dist/index.html    x.xx kB
✓ built in x.xxs
```

✅ **Если видите "built in"** - отлично, проект готов к публикации!

❌ **Если ошибки** - внимательно прочитайте текст ошибки и проверьте шаги 6-8

---

# ЧАСТЬ 4️⃣: Создание аккаунта на GitHub

## Шаг 11: Зарегистрируйтесь на GitHub

1. **Откройте браузер**

2. **Перейдите на**: https://github.com

3. **Нажмите** кнопку **"Sign up"** (Регистрация)

4. **Заполните форму**:
   - **Email**: ваш email (тот же, что указали в Git!)
   - **Password**: придумайте надёжный пароль
   - **Username**: придумайте никнейм (например: maria-baker)
     - Только латинские буквы, цифры и дефис
     - Запомните его - он будет в вашей ссылке!

5. **Подтвердите email**:
   - Откройте почту
   - Найдите письмо от GitHub
   - Кликните на ссылку подтверждения

6. **Войдите на GitHub** с вашим логином и паролем

✅ Готово! У вас есть аккаунт на GitHub.

---

## Шаг 12: Создайте репозиторий

**Репозиторий** - это "место" на GitHub, где будет храниться ваш код.

1. **На GitHub** нажмите кнопку **"+"** (плюс) в правом верхнем углу

2. **Выберите** "New repository"

3. **Заполните форму**:

   **Repository name** (название):
   ```
   sweet-calculator
   ```
   (или другое название, которое вы указали в конфигах на Шаге 8)

   **Description** (описание) - необязательно:
   ```
   Калькулятор себестоимости для кондитеров
   ```

   **Public** - оставьте эту галочку (приложение будет доступно всем)

   **⚠️ ВАЖНО - НЕ СТАВЬТЕ галочки на**:
   - ❌ Add a README file
   - ❌ Add .gitignore  
   - ❌ Choose a license

   Всё должно быть пустым!

4. **Нажмите зелёную кнопку** "Create repository"

5. **Откроется страница** с инструкциями и URL

6. **Найдите и СКОПИРУЙТЕ URL** (выглядит так):
   ```
   https://github.com/ваш-username/sweet-calculator.git
   ```

   Например:
   ```
   https://github.com/maria-baker/sweet-calculator.git
   ```

7. **Сохраните этот URL** в блокноте - он нужен в следующем шаге!

✅ Репозиторий создан!

---

# ЧАСТЬ 5️⃣: Публикация приложения

## Шаг 13: Загрузите код на GitHub

**Вернитесь в терминал** (который открыт в папке проекта)

Сейчас вы введёте **6 команд по очереди**. После каждой нажимайте Enter!

### Команда 1:
```
git init
```
(Инициализация Git)

### Команда 2:
```
git add .
```
(Добавление всех файлов. Точка в конце - часть команды!)

### Команда 3:
```
git commit -m "Initial commit"
```
(Создание "снимка" кода)

### Команда 4:
```
git branch -M main
```
(Переименование ветки)

### Команда 5:
```
git remote add origin https://github.com/ваш-username/sweet-calculator.git
```

**⚠️ ЗАМЕНИТЕ** URL на тот, который вы скопировали в Шаге 11!

Например:
```
git remote add origin https://github.com/maria-baker/sweet-calculator.git
```

### Команда 6:
```
git push -u origin main
```
(Загрузка кода на GitHub)

**ЧТО МОЖЕТ ПРОИЗОЙТИ:**

#### ✅ Вариант А: Всё прошло успешно
Вы увидите процесс загрузки и в конце:
```
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

#### 🔐 Вариант Б: Просит логин и пароль

GitHub может попросить авторизацию:

1. **Username**: введите ваш никнейм на GitHub

2. **Password**: НЕ вводите пароль! Нужен **токен**

**Как получить токен:**

1. Откройте в браузере: https://github.com/settings/tokens

2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**

3. Заполните:
   - **Note**: `Sweet Calculator Deploy`
   - **Expiration**: `90 days`
   - **Scopes**: поставьте галочку **repo** (самая первая)

4. Прокрутите вниз и нажмите **"Generate token"**

5. **СКОПИРУЙТЕ ТОКЕН!** (показывается только один раз!)
   - Выглядит так: `ghp_xxxxxxxxxxxxxxxxxx`

6. **Вставьте токен** в терминал вместо пароля
   - Нажмите Enter

7. Загрузка продолжится

✅ Готово! Код загружен на GitHub!

---

## Шаг 13: Опубликуйте сайт

**В терминале** введите последнюю команду:

```
npm run deploy
```

**Нажмите Enter**

**ЧТО ПРОИСХОДИТ:**
- Проект собирается
- Загружается на специальную ветку gh-pages
- Это занимает 1-3 минуты

**Когда увидите**:
```
Published
```

**Значит всё готово!** 🎉

---

## Шаг 14: Включите GitHub Pages

Последний шаг!

1. **В браузере** перейдите на:
   ```
   https://github.com/ваш-username/sweet-calculator
   ```

2. **Нажмите** вкладку **"Settings"** (Настройки)

3. **В левом меню** найдите и нажмите **"Pages"**

4. **В разделе "Build and deployment"**:
   - **Source**: выберите **"Deploy from a branch"**
   - **Branch**: выберите **gh-pages** (НЕ main!)
   - **Folder**: оставьте **/ (root)**
   - **Нажмите Save**

5. **Подождите 1-2 минуты**

6. **Обновите страницу** (F5)

7. **Вы увидите зелёную галочку** и ссылку:
   ```
   ✅ Your site is live at https://ваш-username.github.io/sweet-calculator/
   ```

8. **СКОПИРУЙТЕ ССЫЛКУ!** Это ссылка на ваше приложение!

---

# 🎉 ГОТОВО! Поздравляю!

## 🎊 Что вы сделали:

- ✅ Установили все нужные программы
- ✅ Скачали и настроили проект
- ✅ Создали аккаунт на GitHub
- ✅ Опубликовали приложение в интернете
- ✅ Получили красивую ссылку на ваше приложение!

## 🌐 Что теперь делать:

### Откройте ваше приложение:

1. **Скопируйте ссылку** из Шага 14

2. **Откройте её в браузере**

3. **Приложение работает!** 🎊

### На телефоне:

1. **Откройте ту же ссылку на телефоне**

2. **Появится предложение** "Добавить на главный экран"

3. **Добавьте!**

4. **Теперь у вас есть приложение** на телефоне!

### Поделитесь с друзьями:

Отправьте ссылку друзьям-кондитерам!

---

# 🔄 Как обновить приложение

Если вы что-то изменили в коде:

1. **Откройте терминал** в папке проекта

2. **Введите 3 команды**:
   ```
   git add .
   git commit -m "Обновление"
   npm run deploy
   ```

3. **Подождите 1-2 минуты**

4. **Готово!** Изменения на сайте

---

# 🆘 Если что-то пошло не так

## "npm command not found"
- Переустановите Node.js
- Перезагрузите компьютер
- Откройте НОВ��Й терминал

## "git command not found"
- Переустановите Git
- Перезагрузите компьютер
- Откройте НОВЫЙ терминал

## Сайт показывает 404
- Проверьте Settings → Pages
- Убедитесь, что выбрана ветка **gh-pages**
- Подождите 5 минут
- Обновите страницу (Ctrl+Shift+R)

## Белый экран
- Проверьте `vite.config.ts` (base)
- Проверьте `package.json` (homepage)
- Убедитесь, что названия совпадают
- Выполните `npm run deploy` снова

## Терминал не открывается в папке (Windows)
1. Откройте папку в Проводнике
2. Кликните в адресную строку (где путь)
3. Введите: `cmd`
4. Нажмите Enter

---

# 💡 Полезные советы

1. **Сохраняйте токен GitHub** в надёжном месте
2. **Делайте резервные копии** проекта
3. **Пишите понятные сообщения** при обновлениях
4. **Тестируйте изменения** командой `npm run build` перед публикацией

---

# ✨ Вы молодец!

Вы только что:
- 🎓 Научились работать с GitHub
- 💻 Опубликовали настоящее приложение
- 📱 Создали PWA для телефона
- 🌐 Получили свою ссылку в интернете

**Это серьёзное достижение!** 🏆

Теперь вы можете создавать и публиковать свои приложения!

---

**Приятного использования "Сладкого расчёта"!** 🧁💜

*Сделано с любовью для начинающих разработчиков* 💙