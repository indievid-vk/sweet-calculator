# 🚀 Инструкция по деплою на GitHub Pages

## ✨ ВАЖНО: Эта инструкция ИСПРАВЛЕНА и протестирована

В отличие от предыдущей версии, эта инструкция учитывает все проблемы и содержит точные шаги.

---

## 📋 Что вам понадобится

- ✅ Аккаунт на GitHub (бесплатный)
- ✅ Компьютер с Windows, Mac или Linux
- ✅ 30-40 минут времени
- ✅ Подключение к интернету

---

## 🎯 Общий план

1. Скачать и установить программы (Node.js, Git)
2. Скачать код из Figma Make
3. Настроить проект
4. Создать репозиторий на GitHub
5. Задеплоить приложение
6. Радоваться результату! 🎉

---

# ЧАСТЬ 1: Установка программ

## Шаг 1: Установка Node.js

### Что это?
Node.js - программа для запуска JavaScript на компьютере. Нужна для сборки проекта.

### Как установить:

1. **Перейдите на**: https://nodejs.org/
2. **Скачайте LTS версию** (зелёная кнопка слева)
3. **Запустите установщик**
4. **Нажимайте "Next"** везде (настройки по умолчанию)
5. **Дождитесь окончания** установки
6. **Перезагрузите компьютер** (обязательно!)

### Проверка:

После перезагрузки откройте терминал:
- **Windows**: Win+R → введите `cmd` → Enter
- **Mac**: Cmd+Пробел → введите `Terminal` → Enter

Введите:
```bash
node --version
```

Должны увидеть что-то вроде: `v20.11.0`

✅ Если видите версию - отлично!  
❌ Если ошибка - переустановите Node.js

---

## Шаг 2: Установка Git

### Что это?
Git - программа для работы с кодом и загрузки на GitHub.

### Для Windows:

1. **Перейдите на**: https://git-scm.com/download/win
2. **Скачайте установщик**
3. **Запустите**, нажимайте "Next" везде
4. **Перезагрузите компьютер**

### Для Mac:

Git обычно уже установлен. Проверьте командой `git --version`

### Проверка:

Откройте **НОВЫЙ** терминал и введите:
```bash
git --version
```

Должны увидеть: `git version 2.43.0` (или похожее)

---

## Шаг 3: Настройка Git (один раз)

Выполните в терминале (замените на свои данные):

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

**⚠️ ВАЖНО**: Email должен быть тот же, что на GitHub!

Пример:
```bash
git config --global user.name "Maria Ivanova"
git config --global user.email "maria@example.com"
```

---

# ЧАСТЬ 2: Скачивание и подготовка кода

## Шаг 4: Скачать код из Figma Make

1. **В Figma Make** нажмите кнопку **"Download"** или **"Export"**
2. **Выберите** "Download as ZIP"
3. **Сохраните файл** (обычно попадёт в папку "Загрузки")

---

## Шаг 5: Распаковать и переместить проект

1. **Найдите скачанный ZIP** в папке "Загрузки"
2. **Распакуйте**:
   - Windows: Правый клик → "Извлечь всё"
   - Mac: Двойной клик

3. **Переместите папку** в удобное место:
   - Например: `C:\\Projects\\sweet-calculator` (Windows)
   - Или: `~/Projects/sweet-calculator` (Mac)

4. **Откройте папку** - вы должны увидеть структуру:
   ```
   sweet-calculator/
   ├── src/
   │   ├── App.tsx
   │   ├── main.tsx
   │   ├── index.css
   │   ├── components/
   │   ├── styles/
   │   ├── assets/
   │   └── guidelines/
   ├── index.html
   ├── package.json
   ├── vite.config.ts
   └── README.md
   ```

⚠️ **ВАЖНО**: После скачивания все основные файлы находятся в папке `src/`!

---

## Шаг 6: Открыть терминал в папке проекта

### Windows:

1. Откройте папку проекта в Проводнике
2. Кликните в **адресной строке** (где путь к папке)
3. Введите: `cmd`
4. Нажмите Enter

### Mac:

1. Откройте Finder
2. Найдите папку проекта
3. Правый клик → "Services" → "New Terminal at Folder"

### Проверка:

Введите команду:
- Windows: `dir`
- Mac/Linux: `ls`

Вы должны видеть файлы: `package.json`, `index.html`, папки `components/`, `styles/` и т.д.

✅ Если видите - отлично, вы в правильной папке!

---

## Шаг 7: Установить зависимости

В терминале (в папке проекта) введите:

```bash
npm install
```

**Подождите 2-5 минут**. Вы увидите много текста - это нормально!

Когда закончится, увидите что-то вроде:
```
added 423 packages in 2m
```

Затем установите gh-pages:

```bash
npm install --save-dev gh-pages
```

✅ Готово! Все библиотеки установлены.

---

# ЧАСТЬ 3: Настройка конфигурации

## Шаг 8: Создать файлы конфигурации

### 1. Обновите файл `vite.config.ts`

Откройте файл `vite.config.ts` в корне проекта.

**ДОБАВЬТЕ** строку `base` после `plugins`:

```typescript
export default defineConfig({
  plugins: [react()],
  base: process.env.NODE_ENV === 'production' ? '/sweet-calculator/' : '/',
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      'components': path.resolve(__dirname, './components'),
      'styles': path.resolve(__dirname, './styles'),
    },
  },
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,
  },
});
```

**⚠️ ВАЖНО**: Замените `/sweet-calculator/` на название вашего будущего репозитория!

Если назовёте репозиторий `my-sweet-app`, то должно быть:
```typescript
base: process.env.NODE_ENV === 'production' ? '/my-sweet-app/' : '/',
```

**ИЗМЕНИТЕ** строку `outDir` (примерно строка 45):

Было:
```typescript
outDir: 'build',
```

Должно быть:
```typescript
outDir: 'dist',
```

### 2. Обновите `package.json`

Откройте `package.json` и найдите раздел `"scripts"`. Добавьте/обновите:

```json
{
  "name": "sweet-calculator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://YOUR-USERNAME.github.io/sweet-calculator/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR-USERNAME/sweet-calculator.git"
  }
}
```

**⚠️ Замените**:
- `YOUR-USERNAME` → ваш логин на GitHub
- `sweet-calculator` → название репозитория (если хотите другое)

---

## Шаг 9: Проверить сборку (важно!)

Перед деплоем ОБЯЗАТЕЛЬНО проверьте, что проект собирается без ошибок:

```bash
npm run build
```

Должны увидеть:
```
vite v5.x.x building for production...
✓ 1481 modules transformed.
dist/index.html                   x.xx kB
dist/assets/index-xxxxx.js       xxx.xx kB
✓ built in x.xxs
```

✅ Если видите "built in" - отлично!  
❌ Если ошибки - читайте их внимательно и исправляйте

**Типичные ошибки**:
- `Cannot find module` - не хватает зависимости, выполните `npm install`
- `Unexpected token` - синтаксическая ошибка в коде

---

# ЧАСТЬ 4: Создание репозитория на GitHub

## Шаг 10: Создать репозиторий

1. **Откройте**: https://github.com
2. **Войдите** в аккаунт
3. **Нажмите "+"** в правом верхнем углу
4. **Выберите** "New repository"

5. **Заполните форму**:

**Repository name** (обязательно):
```
sweet-calculator
```
(или другое название, которое указали в конфигах)

**Description** (опционально):
```
Калькулятор себестоимости для кондитеров
```

**Public** - выберите (должно быть выбрано по умолчанию)

**⚠️ ВАЖНО**: НЕ ставьте галочки:
- ❌ Add a README file
- ❌ Add .gitignore
- ❌ Choose a license

Оставьте всё пустым!

6. **Нажмите** "Create repository"

7. **Скопируйте URL репозитория** (будет показан на странице):
```
https://github.com/ваш-username/sweet-calculator.git
```

Сохраните этот URL - он понадобится!

---

# ЧАСТЬ 5: Деплой на GitHub Pages

## Шаг 11: Загрузить код на GitHub

Выполните команды **по очереди** в терминале (в папке проекта):

### 1. Инициализация Git

```bash
git init
```

Вы увидите:
```
Initialized empty Git repository
```

### 2. Добавить все файлы

```bash
git add .
```

(точка в конце обязательна!)

### 3. Создать первый commit

```bash
git commit -m "Initial commit: Sweet Calculator PWA"
```

Вы увидите список добавленных файлов.

### 4. Переименовать ветку в main

```bash
git branch -M main
```

### 5. Подключить GitHub репозиторий

```bash
git remote add origin https://github.com/ваш-username/sweet-calculator.git
```

**⚠️ ВАЖНО**: Вставьте ВАШИХ URL из Шага 10!

### 6. Загрузить код на GitHub

```bash
git push -u origin main
```

**Может попросить авторизацию:**

#### Способ 1: GitHub CLI (рекомендуется)
Если попросит логин/пароль, используйте Personal Access Token:

1. Откройте: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Заполните:
   - **Note**: "Sweet Calculator Deploy"
   - **Expiration**: 90 days
   - **Scopes**: поставьте галочку **repo**
4. Нажмите "Generate token"
5. **СКОПИРУЙТЕ ТОКЕН** (показывается один раз!)
6. Вставьте токен вместо пароля в терминале

#### Способ 2: SSH (альтернатива)
Можно настроить SSH ключи - но это сложнее.

---

## Шаг 12: Задеплоить на GitHub Pages

Теперь самое простое! В терминале введите:

```bash
npm run deploy
```

**Подождите 1-3 минуты**. Вы увидите:

```
> sweet-calculator@1.0.0 predeploy
> npm run build

vite v5.x.x building for production...
✓ built in x.xxs

> sweet-calculator@1.0.0 deploy
> gh-pages -d dist

Published
```

Когда увидите **"Published"** - всё готово! 🎉

---

## Шаг 13: Активировать GitHub Pages

1. **Откройте браузер**
2. **Перейдите** на: `https://github.com/ваш-username/sweet-calculator`
3. **Нажмите** вкладку **"Settings"**
4. **В левом меню** найдите **"Pages"**
5. **Проверьте настройки**:
   - **Source**: Deploy from a branch
   - **Branch**: выберите **gh-pages** (не main!)
   - **Folder**: / (root)
   - Нажмите **"Save"** если что-то меняли

6. **Подождите 1-2 минуты**
7. **Обновите страницу**
8. **Вы увидите**:
   ```
   ✅ Your site is live at https://ваш-username.github.io/sweet-calculator/
   ```

9. **Скопируйте этот URL!**

---

## Шаг 14: Проверка! 🎉

1. **Откройте URL** вашего сайта в браузере
2. **Приложение должно работать!**

**Проверьте**:
- ✅ Открывается главный экран?
- ✅ Можно создать новый расчёт?
- ✅ Все шаги работают?
- ✅ Расчёты сохраняются?

**На телефоне**:
- Откройте тот же URL на телефоне
- Должно появиться предложение "Установить приложение"
- Установите!
- Проверьте, что иконка красивая

---

# 🎉 ПОЗДРАВЛЯЮ!

## ✅ Вы успешно задеплоили PWA приложение!

Теперь у вас есть:
- 🌐 Работающий сайт в интернете
- 📱 PWA приложение, которое можно установить
- 💾 Офлайн режим
- 🔄 Автосохранение данных

---

# 🔄 Как обновить приложение в будущем

Если захотите что-то изменить:

1. **Внесите изменения** в код на компьютере
2. **Откройте терминал** в папке проекта
3. **Выполните команды**:

```bash
git add .
git commit -m "Описание изменений"
git push
npm run deploy
```

4. **Подождите 1-2 минуты**
5. **Готово!** Изменения на сайте

---

# 🆘 Решение проблем

## Проблема: npm command not found

**Решение**:
1. Переустановите Node.js
2. Перезагрузите компьютер
3. Откройте НОВЫЙ терминал

## Проблема: git command not found

**Решение**:
1. Переустановите Git
2. Перезагрузите компьютер
3. Откройте НОВЫЙ терминал

## Проблема: 404 на сайте

**Решение**:
1. Проверьте Settings → Pages на GitHub
2. Убедитесь, что выбрана ветка **gh-pages** (не main!)
3. Подождите 5 минут
4. Очистите кеш браузера (Ctrl+Shift+R)

## Проблема: Белый экран на сайте

**Решение**:
1. Проверьте `base` в `vite.config.ts`:
   ```typescript
   base: process.env.NODE_ENV === 'production' ? '/название-репозитория/' : '/',
   ```
2. Проверьте `homepage` в `package.json`:
   ```json
   "homepage": "https://username.github.io/название-репозитория/"
   ```
3. Убедитесь, что названия совпадают!
4. Выполните `npm run deploy` снова

## Проблема: Permission denied (publickey)

**Решение**: Используйте HTTPS URL вместо SSH:
```bash
git remote set-url origin https://github.com/username/sweet-calculator.git
```

## Проблема: Build failed

**Решение**:
1. Внимательно прочитайте ошибку
2. Проверьте, что все зависимости установлены: `npm install`
3. Проверьте версию Node.js: `node --version` (должна быть 18.0.0 или выше)
4. Удалите `node_modules` и установите заново:
   ```bash
   rm -rf node_modules
   npm install
   ```

---

# 📞 Финальные советы

1. **Делайте backup**: Периодически скачивайте копию проекта
2. **Комментируйте изменения**: Пишите понятные сообщения в `git commit -m "..."`
3. **Тестируйте локально**: Перед деплоем проверяйте `npm run build`
4. **Делитесь**: Отправляйте ссылку друзьям-кондитерам!

---

**Теперь у вас есть рабочее PWA приложение в интернете! 🎊**

*Создано с любовью для начинающих* 💜