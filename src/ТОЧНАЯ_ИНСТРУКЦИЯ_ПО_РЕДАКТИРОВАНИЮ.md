# 📝 ТОЧНАЯ инструкция: Что редактировать в файлах

## ⚠️ ЭТО САМАЯ ВАЖНАЯ ИНСТРУКЦИЯ!

Здесь написано **ТОЧНО**, что нужно изменить в файлах после скачивания архива.

---

## 📄 Файл 1: `vite.config.ts`

### Что есть СЕЙЧАС в файле:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react-swc';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    extensions: ['.js', '.jsx', '.ts', '.tsx', '.json'],
    alias: {
      // ... много строк ...
      '@': path.resolve(__dirname, './src'),
    },
  },
  build: {
    target: 'esnext',
    outDir: 'build',
  },
  server: {
    port: 3000,
    open: true,
  },
});
```

### Что нужно ИЗМЕНИТЬ:

#### Изменение 1: Добавить `base`

**Найдите строку:**
```typescript
export default defineConfig({
  plugins: [react()],
```

**ДОБАВЬТЕ** после `plugins: [react()],` новую строку:
```typescript
export default defineConfig({
  plugins: [react()],
  base: process.env.NODE_ENV === 'production' ? '/sweet-calculator/' : '/',
```

**⚠️ ВАЖНО:** Замените `/sweet-calculator/` на название вашего GitHub репозитория!

#### Изменение 2: Исправить `outDir`

**Найдите строку:**
```typescript
outDir: 'build',
```

**Замените на:**
```typescript
outDir: 'dist',
```

### Итоговый результат:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react-swc';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  base: process.env.NODE_ENV === 'production' ? '/sweet-calculator/' : '/',  // ← ДОБАВЛЕНО
  resolve: {
    extensions: ['.js', '.jsx', '.ts', '.tsx', '.json'],
    alias: {
      // ... много строк ...
      '@': path.resolve(__dirname, './src'),
    },
  },
  build: {
    target: 'esnext',
    outDir: 'dist',  // ← ИЗМЕНЕНО с 'build' на 'dist'
  },
  server: {
    port: 3000,
    open: true,
  },
});
```

---

## 📄 Файл 2: `package.json`

### Что есть СЕЙЧАС в файле:

```json
{
  "name": "Sweet cacl",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@radix-ui/react-accordion": "^1.2.3",
    // ... много зависимостей ...
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitejs/plugin-react-swc": "^3.10.2",
    "vite": "6.3.5"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  }
}
```

### Что нужно ИЗМЕНИТЬ:

#### Изменение 1: Добавить `homepage` и `type`

**Найдите строку:**
```json
"private": true,
```

**ДОБАВЬТЕ** после неё две новые строки:
```json
"private": true,
"homepage": "https://YOUR-USERNAME.github.io/sweet-calculator/",
"type": "module",
```

**⚠️ ВАЖНО:** 
- Замените `YOUR-USERNAME` на ваш логин GitHub!
- Замените `sweet-calculator` на название вашего репозитория (если другое)!

#### Изменение 2: Расширить раздел `scripts`

**Найдите:**
```json
"scripts": {
  "dev": "vite",
  "build": "vite build"
}
```

**Замените на:**
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}
```

#### Изменение 3: Добавить раздел `repository`

**ПОСЛЕ раздела `scripts` (после закрывающей `}`), ДОБАВЬТЕ:**

```json
"scripts": {
  ...
},
"repository": {
  "type": "git",
  "url": "https://github.com/YOUR-USERNAME/sweet-calculator.git"
}
```

**⚠️ ВАЖНО:** Замените `YOUR-USERNAME` на ваш логин GitHub!

### Итоговый результат:

```json
{
  "name": "sweet-calculator",
  "version": "1.2.0",
  "private": true,
  "homepage": "https://YOUR-USERNAME.github.io/sweet-calculator/",  // ← ДОБАВЛЕНО
  "type": "module",  // ← ДОБАВЛЕНО
  "dependencies": {
    "@radix-ui/react-accordion": "^1.2.3",
    // ... все зависимости остаются ...
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitejs/plugin-react-swc": "^3.10.2",
    "vite": "6.3.5",
    "gh-pages": "^6.1.0"  // ← будет добавлено автоматически после npm install
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",          // ← ДОБАВЛЕНО
    "predeploy": "npm run build",       // ← ДОБАВЛЕНО
    "deploy": "gh-pages -d dist"        // ← ДОБАВЛЕНО
  },
  "repository": {                       // ← ДОБАВЛЕНО
    "type": "git",
    "url": "https://github.com/YOUR-USERNAME/sweet-calculator.git"
  }
}
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕНЫ

Во всех файлах замените:

1. **`YOUR-USERNAME`** → ваш логин на GitHub
2. **`sweet-calculator`** → название вашего репозитория (если хотите другое)

**Пример:**

Если ваш логин: `maria-baker`  
И репозиторий: `my-sweet-app`

То должно быть:

```json
"homepage": "https://maria-baker.github.io/my-sweet-app/",
```

```typescript
base: process.env.NODE_ENV === 'production' ? '/my-sweet-app/' : '/',
```

```json
"url": "https://github.com/maria-baker/my-sweet-app.git"
```

---

## ✅ Checklist перед деплоем

Проверьте, что вы:

- [ ] Добавили `base` в `vite.config.ts`
- [ ] Изменили `outDir: 'build'` на `outDir: 'dist'` в `vite.config.ts`
- [ ] Добавили `homepage` в `package.json`
- [ ] Добавили `type: "module"` в `package.json`
- [ ] Расширили раздел `scripts` в `package.json`
- [ ] Добавили раздел `repository` в `package.json`
- [ ] Заменили `YOUR-USERNAME` на ваш логин
- [ ] Название репозитория одинаковое везде

---

## 🎯 Теперь можно продолжить деплой!

После внесения изменений:

1. Сохраните оба файла
2. Выполните `npm install`
3. Выполните `npm run build` (проверка)
4. Продолжите по инструкции [DEPLOY_GUIDE_SIMPLE.md](DEPLOY_GUIDE_SIMPLE.md)

---

**Последнее обновление:** 09.11.2024  
**Проверено на реальной структуре архива Figma Make**
