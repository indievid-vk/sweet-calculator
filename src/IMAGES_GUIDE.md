# 📸 Инструкция по работе с изображениями

## 🎯 Общая информация

В приложении "Сладкий расчёт" изображения используются для улучшения визуального восприятия и создания привлекательного интерфейса.

---

## 📁 Структура размещения изображений

### Для локальной разработки (Figma Make)

Изображения уже встроены в проект через специальные импорты Figma:

```typescript
import heroImage from 'figma:asset/821ead3fe60f39c51521599a0cf38d20c01ec99c.png';
```

**Эти импорты работают только в среде Figma Make!**

---

### Для GitHub Pages и самостоятельного деплоя

Для деплоя на GitHub Pages нужно **заменить** импорты Figma на обычные изображения.

#### Шаг 1: Создайте папку для изображений

В корне проекта создайте структуру:

```
/public
  /images
    hero.png
    icon-192.png
    icon-512.png
```

#### Шаг 2: Разместите изображения

1. **Скачайте изображение** из Figma Make (если используете):
   - Откройте превью изображения
   - Правый клик → "Сохранить изображение как..."
   - Сохраните в `/public/images/hero.png`

2. **Или используйте своё изображение**:
   - Рекомендуемый размер: 512x512px
   - Формат: PNG с прозрачностью
   - Тематика: кондитерские изделия, калькулятор

#### Шаг 3: Обновите импорт в коде

**Было (Figma Make):**
```typescript
import heroImage from 'figma:asset/821ead3fe60f39c51521599a0cf38d20c01ec99c.png';
```

**Стало (для деплоя):**
```typescript
// В файле /components/HomePage.tsx

// Удалите строку:
// import heroImage from 'figma:asset/...';

// Замените на:
const heroImage = '/images/hero.png';
```

**Полный код замены:**

```typescript
// Было:
import heroImage from 'figma:asset/821ead3fe60f39c51521599a0cf38d20c01ec99c.png';

// Стало:
const heroImage = '/images/hero.png';

// В JSX остается без изменений:
<img src={heroImage} alt="..." />
```

---

## 🎨 Рекомендации по изображениям

### Главное изображение (hero.png)

**Характеристики:**
- **Размер**: 512x512px или 800x800px
- **Формат**: PNG
- **Цветовая гамма**: соответствует палитре приложения
  - Фон: светло-голубой (#D5E8F0)
  - Акценты: фиолетовый (#9D7AB8), розовый (#E8A8C8), оранжевый (#F8C880)
- **Содержание**: калькулятор, кондитерские изделия, капкейки

**Текущее изображение:**
- Телефон с калькулятором
- Капкейки вокруг
- Ягоды (клубника, черника)
- Мягкие тени

### PWA иконки

Для PWA приложения нужны две иконки:

**icon-192.png:**
- Размер: 192x192px
- Формат: PNG
- Использование: иконка приложения

**icon-512.png:**
- Размер: 512x512px
- Формат: PNG
- Использование: экран загрузки, иконка в магазине

---

## 🔧 Примеры использования

### 1. Импорт изображения

```typescript
// Из папки public (правильно для деплоя)
const image = '/images/hero.png';

// Или через import (если собираете через Vite)
import image from '/public/images/hero.png';
```

### 2. Использование в компоненте

```typescript
export function HomePage() {
  const heroImage = '/images/hero.png';
  
  return (
    <img
      src={heroImage}
      alt="Калькулятор для кондитеров"
      className="w-full h-auto rounded-2xl"
    />
  );
}
```

### 3. С компонентом ImageWithFallback

```typescript
import { ImageWithFallback } from './components/figma/ImageWithFallback';

<ImageWithFallback
  src="/images/hero.png"
  alt="Описание"
  className="w-full"
/>
```

---

## 📋 Чеклист для деплоя

Перед деплоем на GitHub Pages:

- [ ] Создана папка `/public/images/`
- [ ] Размещено изображение `hero.png`
- [ ] Размещены PWA иконки `icon-192.png` и `icon-512.png`
- [ ] В `/components/HomePage.tsx` заменён импорт Figma на обычный путь
- [ ] Проверено, что изображение отображается локально
- [ ] Выполнена команда `npm run build`
- [ ] Нет ошибок при сборке

---

## 🎯 Где используются изображения

### 1. Главная страница (`/components/HomePage.tsx`)

```typescript
const heroImage = '/images/hero.png';

<img
  src={heroImage}
  alt="Калькулятор для кондитеров"
  className="w-full max-w-sm rounded-2xl"
/>
```

### 2. PWA манифест (`/public/manifest.json`)

```json
{
  "icons": [
    {
      "src": "/images/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/images/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

---

## 🐛 Решение проблем

### Изображение не отображается

**Проблема**: Вижу иконку "сломанного изображения"

**Решение**:
1. Проверьте путь к файлу
2. Убедитесь, что файл в папке `/public/images/`
3. Проверьте название файла (регистр важен!)
4. Очистите кеш: `Ctrl+Shift+R` (Windows) или `Cmd+Shift+R` (Mac)

### Ошибка при сборке

**Проблема**: `Module not found: Can't resolve 'figma:asset/...'`

**Решение**:
1. Замените все `figma:asset/...` импорты на обычные пути
2. Пример: `import img from 'figma:asset/...'` → `const img = '/images/hero.png'`

### Изображение есть локально, но не на GitHub Pages

**Проблема**: Локально работает, после деплоя - нет

**Решение**:
1. Проверьте, что файл закоммичен в Git:
   ```bash
   git status
   ```
2. Если файл не добавлен:
   ```bash
   git add public/images/
   git commit -m "Add images"
   git push
   npm run deploy
   ```

---

## 💡 Полезные советы

1. **Оптимизация**: Сжимайте PNG изображения через tinypng.com
2. **Формат**: Для иконок лучше использовать PNG с прозрачностью
3. **Размер**: Не используйте слишком большие изображения (макс. 500KB)
4. **Alt текст**: Всегда указывайте понятный alt для доступности
5. **Responsive**: Используйте max-width для адаптивности

---

## ✅ Готовый пример

**Структура файлов:**
```
/public
  /images
    hero.png          (512x512px, ~200KB)
    icon-192.png      (192x192px, ~50KB)
    icon-512.png      (512x512px, ~150KB)
```

**Код HomePage.tsx:**
```typescript
import { Button } from './ui/button';
import { Plus, Calculator, History } from 'lucide-react';

// Используем простой путь к изображению
const heroImage = '/images/hero.png';

export function HomePage({ calculationsCount, onNewCalculation, onViewCalculations }) {
  return (
    <div className="flex flex-col items-center space-y-8 py-8">
      <div className="w-full max-w-sm">
        <img
          src={heroImage}
          alt="Калькулятор для кондитеров"
          className="w-full h-auto rounded-2xl"
        />
      </div>
      {/* ... остальной код ... */}
    </div>
  );
}
```

---

**Готово!** Теперь изображения будут работать и после деплоя на GitHub Pages! 🎉
