# 📂 Правильная структура проекта после скачивания из Figma Make

## ✅ РЕАЛЬНАЯ структура после скачивания

После того, как вы скачали и распаковали архив из Figma Make, вы увидите следующую структуру:

```
sweet-calculator/
├── src/                         # ← Все файлы проекта ЗДЕСЬ!
│   ├── App.tsx                  # Главный компонент
│   ├── main.tsx                 # Точка входа
│   ├── index.css                # Базовые стили
│   ├── components/              # Все компоненты
│   │   ├── HomePage.tsx
│   │   ├── RecipeForm.tsx
│   │   ├── Calculation.tsx
│   │   ├── MyCalculationsPage.tsx
│   │   └── ui/                  # UI компоненты
│   ├── styles/                  # Стили
│   │   └── globals.css
│   ├── assets/                  # Ресурсы
│   └── guidelines/              # Документация
├── index.html                   # HTML в корне
├── package.json                 # Зависимости в корне
├── vite.config.ts              # Конфиг Vite в корне
└── README.md                    # Документация
```

## ⚠️ Важно понять

**Это СТАНДАРТНАЯ структура Vite + React!**

Все основные файлы находятся в папке `src/`, а конфигурационные файлы (`index.html`, `package.json`, `vite.config.ts`) - в корне проекта.

---

## 📝 Что нужно сделать после распаковки

### 1. Проверить файл `vite.config.ts`

Откройте файл `vite.config.ts` в корне проекта и убедитесь, что он правильно настроен:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  base: process.env.NODE_ENV === 'production' ? '/ВАШ-РЕПОЗИТОРИЙ/' : '/',
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  },
});
```

**⚠️ Замените `/ВАШ-РЕПОЗИТОРИЙ/` на название вашего GitHub репозитория!**

### 2. Создать папку `public/` (если её нет)

Для PWA манифеста и иконок создайте папку `public` в корне:

```
sweet-calculator/
├── src/
├── public/              # ← Создайте эту папку
│   ├── manifest.json    # Для PWA
│   └── icons/          # Для иконок приложения
├── index.html
└── package.json
```

### 3. Проверить `package.json`

Убедитесь, что в `package.json` есть необходимые поля:

```json
{
  "name": "sweet-calculator",
  "homepage": "https://ВАШ-USERNAME.github.io/ВАШ-РЕПОЗИТОРИЙ/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

### 4. Установить зависимости

```bash
npm install
npm install --save-dev gh-pages
```

### 5. Проверить сборку

```bash
npm run build
```

Если видите `✓ built in X.XXs` - всё отлично!

---

## 🐛 Частые ошибки

### Ошибка: "Cannot find module '@/...'"

**Решение**: Проверьте алиас в `vite.config.ts`:
```typescript
'@': path.resolve(__dirname, './src')  // Должен указывать на ./src
```

### Ошибка: Белый экран после деплоя

**Решение**: Проверьте `base` в `vite.config.ts` - он должен совпадать с названием репозитория.

---

## ✅ Checklist перед деплоем

- [ ] Файл `vite.config.ts` настроен правильно
- [ ] `base` в vite.config.ts совпадает с названием репозитория
- [ ] `homepage` в package.json указан правильно
- [ ] Папка `public/` создана (если нужна для PWA)
- [ ] Команда `npm install` выполнена
- [ ] Команда `npm run build` работает без ошибок

---

## 🎯 Готово к деплою!

Теперь можете продолжить по инструкции:
- [DEPLOY_INSTRUCTIONS.md](DEPLOY_INSTRUCTIONS.md) - полная инструкция
- [DEPLOY_GUIDE_SIMPLE.md](DEPLOY_GUIDE_SIMPLE.md) - для новичков
