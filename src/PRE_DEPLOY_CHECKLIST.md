# ✅ Чеклист перед деплоем

## 📦 Версия 1.2.0

### ✨ Новые возможности в этой версии:
- [x] Экспорт расчётов в Excel (.xlsx)
- [x] Улучшенный PWA Install Dialog с приветствием
- [x] Обновлена документация (README, QUICK_START, USAGE_EXAMPLES)
- [x] Добавлены CHANGELOG.md и RELEASE_NOTES_v1.2.md

### 📚 Используемые библиотеки:
- xlsx (SheetJS) - для экспорта в Excel

## 📋 Проверьте перед публикацией

### 1. Файлы конфигурации

#### vite.config.ts
```typescript
base: process.env.NODE_ENV === 'production' ? '/ВАШ-РЕПОЗИТОРИЙ/' : '/',
```
- [ ] Указано правильное название репозитория
- [ ] Слэши `/` в начале и конце
- [ ] Название совпадает с GitHub репозиторием

#### package.json
```json
"homepage": "https://ВАШ-USERNAME.github.io/ВАШ-РЕПОЗИТОРИЙ/",
"url": "https://github.com/ВАШ-USERNAME/ВАШ-РЕПОЗИТОРИЙ.git"
```
- [ ] Указан правильный username
- [ ] Указано правильное название репозитория
- [ ] Названия совпадают везде

### 2. Установлены зависимости

```bash
npm install
npm install --save-dev gh-pages
```
- [ ] Выполнена команда `npm install`
- [ ] Установлен gh-pages
- [ ] Нет ошибок при установке

### 3. Проект собирается

```bash
npm run build
```
- [ ] Команда выполнилась успешно
- [ ] Видно сообщение "✓ built in X.XXs"
- [ ] Создана папка `dist/`
- [ ] Нет ошибок компиляции

### 4. Git настроен

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```
- [ ] Указано имя
- [ ] Указан email (тот же, что на GitHub!)

### 5. Репозиторий на GitHub создан

- [ ] Создан репозиторий с правильным названием
- [ ] Выбран Public
- [ ] НЕ добавлен README
- [ ] НЕ добавлен .gitignore
- [ ] Скопирован URL репозитория

### 6. Код загружен

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
```
- [ ] Все команды выполнены по порядку
- [ ] Код загружен на GitHub
- [ ] Нет ошибок

### 7. Деплой выполнен

```bash
npm run deploy
```
- [ ] Видно "Published"
- [ ] Нет ошибок

### 8. GitHub Pages настроен

Settings → Pages:
- [ ] Source: Deploy from a branch
- [ ] Branch: **gh-pages** (НЕ main!)
- [ ] Folder: / (root)
- [ ] Нажат Save

### 9. Проверка работы

- [ ] Сайт открывается по ссылке
- [ ] Нет 404 ошибки
- [ ] Нет белого экрана
- [ ] Все функции работают
- [ ] Можно создать расчёт
- [ ] Данные сохраняются

### 10. PWA проверка

- [ ] Предлагает установить на телефоне
- [ ] Устанавливается корректно
- [ ] Иконка отображается
- [ ] Работает офлайн

---

## 🔧 Если что-то не работает

### Белый экран
→ Проверьте `base` в vite.config.ts  
→ Проверьте `homepage` в package.json  
→ Убедитесь, что названия совпадают

### 404 ошибка
→ Проверьте Settings → Pages  
→ Выберите ветку **gh-pages**  
→ Подождите 2-3 минуты

### Build failed
→ Прочитайте ошибку внимательно  
→ Проверьте `npm install`  
→ Проверьте версию Node.js (`node --version`)

### Permission denied
→ Используйте HTTPS URL, не SSH  
→ Создайте Personal Access Token  
→ Используйте токен вместо пароля

---

## ✅ Всё готово!

Если все пункты отмечены - ваше приложение опубликовано! 🎉

**Ссылка**: `https://ваш-username.github.io/ваш-репозиторий/`

---

## 🔄 При обновлениях

Для публикации изменений просто:

```bash
git add .
git commit -m "Описание изменений"
npm run deploy
```

Готово! Изменения будут на сайте через 1-2 минуты.

---

## 🔧 Исправление импортов перед деплоем

### Проблема
Некоторые UI компоненты используют версии в импортах (например, `@radix-ui/react-dialog@1.1.6`), что может вызвать ошибки при деплое.

### Решение

**Замените следующие строки в указанных файлах:**

#### В файле `/components/ui/dialog.tsx`:
```typescript
// Было:
import * as DialogPrimitive from "@radix-ui/react-dialog@1.1.6";
import { XIcon } from "lucide-react@0.487.0";

// Стало:
import * as DialogPrimitive from "@radix-ui/react-dialog";
import { XIcon } from "lucide-react";
```

#### В файле `/components/ui/scroll-area.tsx`:
```typescript
// Было:
import * as ScrollAreaPrimitive from "@radix-ui/react-scroll-area@1.2.3";

// Стало:
import * as ScrollAreaPrimitive from "@radix-ui/react-scroll-area";
```

#### В файле `/components/ui/collapsible.tsx`:
```typescript
// Было:
import * as CollapsiblePrimitive from "@radix-ui/react-collapsible@1.1.3";

// Стало:
import * as CollapsiblePrimitive from "@radix-ui/react-collapsible";
```

#### В файле `/components/ui/separator.tsx`:
```typescript
// Было:
import * as SeparatorPrimitive from "@radix-ui/react-separator@1.1.2";

// Стало:
import * as SeparatorPrimitive from "@radix-ui/react-separator";
```

### Быстрый способ (поиск и замена)

Используйте редактор кода с функцией "Найти и заменить во всех файлах":

1. Найти: `@radix-ui/([^"@]+)@[0-9.]+`
2. Заменить на: `@radix-ui/$1`
3. Включить RegEx
4. Заменить все

Аналогично для lucide-react:

1. Найти: `lucide-react@[0-9.]+`
2. Заменить на: `lucide-react`
3. Заменить все

---

## ✅ Финальная проверка

- [ ] Исправлены все импорты
- [ ] Проект собирается без ошибок
- [ ] Изображения добавлены в /public/images/
- [ ] Все файлы закоммичены
- [ ] Готово к деплою!
